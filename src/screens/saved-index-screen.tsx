import React from 'react';
import { Text } from 'react-native';
import { Layout } from '../components';

export function SavedIndexScreen(): JSX.Element {
  return (
    <Layout>
      <Text>Saved Index</Text>
    </Layout>
  );
}
